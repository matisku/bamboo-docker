machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t matisq/bamboo-server ./bamboo-server/Dockerfile

test:
  override:
    - docker run matisq/bamboo-server
