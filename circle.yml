machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t matisq/bamboo-server bamboo-server

test:
  override:
    - docker run -d -p 8085:8085 matisq/bamboo-server; sleep 30
    - curl --retry 10 --retry-delay 5 -v http://localhost:8085
